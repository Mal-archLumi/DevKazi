import 'package:flutter/material.dart';

class TeamProjectsTab extends StatelessWidget {
  const TeamProjectsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Projects Tab'));
  }
}
