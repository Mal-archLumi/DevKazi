import 'package:flutter/material.dart';

class TeamMembersTab extends StatelessWidget {
  const TeamMembersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Members Tab'));
  }
}
